let numeros = [];

for (let i = 0; i < 5; i++) {
    numeros[i] = parseInt(prompt("Digite um número:"));
}

document.write("Números maiores que 10:<br>");

for (let i = 0; i < numeros.length; i++) {
    if (numeros[i] > 10) {
        document.write(numeros[i] + "<br>");
    }
}
